#include <stdio.h>

void main()
{
    long int f,l=1,i=1;
    printf("enter a number to find its factorial:");
    scanf("%ld",&f);
    for(i;i<=f;i++){
        l=l*i;
    }
    printf("the factorial of %ld is %ld",f,l);
}
