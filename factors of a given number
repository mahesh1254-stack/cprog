//display the factors of a given input number by the user 
#include <stdio.h>

void main()
{
    int a;
    printf("enter a number:");
    scanf("%d",&a);
    printf("Factors are:");
    for(int i=1;i<=a;i++){
        if(a%i==0){
            printf("\n%d",i);
        }
    }
}
